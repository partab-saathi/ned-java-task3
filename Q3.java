/*Q3:  Assign values to the base and height variables and calculate the area.
Write a program that calculates the area of a triangle using the formula:
Area=1/2​(×base×height) */
public class Q3 {

    public static void main(String[] args) {
        int Base = 15;
        int height = 25;
        int Area = (1 / 2) * (Base * height);
        System.out.println("Area = " + (Area));
    }
}
